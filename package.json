{
  "name": "node-excellent-migrations-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "build:core": "bun run --filter node-excellent-migrations build",
    "build:eslint-plugin": "bun run --filter eslint-plugin-node-excellent-migrations build",
    "test": "npm run build && vitest run",
    "test:watch": "vitest",
    "lint": "tsc -b packages/core packages/eslint-plugin --pretty false"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/pg": "^8.20.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
