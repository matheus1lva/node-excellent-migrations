{
  "name": "node-excellent-migrations-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/core packages/eslint-plugin",
    "test": "npm run build && vitest run",
    "test:watch": "vitest",
    "lint": "tsc -b packages/core packages/eslint-plugin --pretty false"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/pg": "^8.20.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
